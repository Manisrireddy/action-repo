# Action Repo

This repository contains a GitHub Actions workflow that triggers on push and pull request events. It demonstrates sending webhook events to a Flask app.

## Workflow
- On push events to the `main` branch.
- On pull requests to the `main` branch.
